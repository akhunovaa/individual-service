kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.volume.claim.name }}
spec:
  storageClassName: {{ .Values.volume.name }}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi