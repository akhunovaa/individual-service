kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.volume.claim.name }}
spec:
  storageClassName: {{ .Values.volume.name }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi